{
  "name": "@clan-world/contracts",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "command -v forge >/dev/null 2>&1 && forge build || echo 'forge not installed; skipping (install via foundryup)'",
    "test": "command -v forge >/dev/null 2>&1 && forge test || echo 'forge not installed; skipping'",
    "typecheck": "echo 'no TS in contracts package'",
    "lint": "echo 'lint stub'",
    "clean": "rm -rf out cache .turbo"
  }
}
